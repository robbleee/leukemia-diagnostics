import re; content = open(\"app.py\", \"r\").read(); pattern = re.compile(r\"    elif sub_tab == \\"Risk\\":(.*?)    elif sub_tab == \\"Differentiation\\":\", re.DOTALL); from_line = re.search(r\"    elif sub_tab == \\"Risk\\":\", content).start(); to_line = re.search(r\"    elif sub_tab == \\"Differentiation\\":\", content).start(); print(f\"Will replace lines from {from_line} to {to_line}\")
